[TOC]

主要的eBPF前端工具有`bcc`和`bpftrace`：`bcc`是一个比较复杂、成熟的工具，如果仅仅是使用的话，推荐使用`bcc`；如果是想要自己编程，实现一些性能分析工具，推荐使用`bpftrace`。

`eBPF`跟踪工具能够干什么？

1. 是否有ext4操作超过50ms？
2. 什么是运行队列延迟直方图？
3. 查看哪些数据包和应用程序正在发生TCP重传？不跟踪（send和receive）。
4. 当线程阻塞（`off-CPU`）时，如何进行堆栈跟踪？线程阻塞的时间是多长？
5. `eBPF`还可以用于安全模块和网络。


